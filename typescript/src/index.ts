const name = "dana",
  age = 24,
  gender = "female";

const sayHi = (name:String, age:number, gender:String) => {
  console.log(`Hello ${name}, you ar ${age}, and ${gender}`);
};

sayHi("dana", 28, "female");

export {};